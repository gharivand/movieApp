import React, {Component} from 'react';
import {View} from 'react-native';
import {ScrollViewContainer} from '../common';

export default class MovieDetails extends Component {
    render() {
        return (
            <ScrollViewContainer>

            </ScrollViewContainer>
        );
    }

}
